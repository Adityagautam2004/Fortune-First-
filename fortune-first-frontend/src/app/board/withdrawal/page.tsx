import { Banknote } from 'lucide-react';
import { ComingSoon } from '@/components/shared/coming-soon';

export default function BoardWithdrawalPage() {
  return (
    <ComingSoon
      icon={Banknote}
      title="Withdrawal"
      description="Client withdrawal requests and processing will show up here once this section is built out."
    />
  );
}
